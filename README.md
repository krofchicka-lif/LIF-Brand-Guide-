# LIF Brand Guide

Deploy on Railway:
1. Upload this folder to a GitHub repo.
2. In Railway, create a new project from the repo.
3. Railway will run `npm install` and `npm start`.

Important: this HTML references an `/assets` folder. Add the real image files into `assets/` before deploying if you want images/logos to load.
